name: CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
      name: Checkout
    - name: Install Docker
      run: sudo apt-get update | sudo apt-get remove docker docker-engine docker.io | sudo apt install docker.io | docker --version
    